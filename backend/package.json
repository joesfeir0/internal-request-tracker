{
  "name": "internal-request-tracker-backend",
  "version": "0.2.0",
  "private": true,
  "description": "Week 2 bounded Service Request lifecycle milestone",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/main.js",
    "test": "npm run build && node --test test/requests.test.cjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@nestjs/common": "^11.1.0",
    "@nestjs/core": "^11.1.0",
    "@nestjs/platform-express": "^11.1.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.9.3"
  }
}
